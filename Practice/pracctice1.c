#include<stdio.h>
int main(){
    float side;
    // calculate:side of area of square
    printf("Enter the side of square: ");
    scanf("%f",&side);
    printf("Area of square: %f",side*side);
    return 0;
}